namespace DomainHelper.Test.Blazor.Store;

public class BlazorWebTest {
    [Fact]
    public async Task CounterTest() {
        //using var ctx = new TestContext();
        //ctx.Services
        //    .AddMemento()
        //    .ScanAssemblyAndAddStores(GetType().Assembly);

        //var cut = ctx.RenderComponent<CounterView>();

        //var store = ctx.Services.GetRequiredService<AsyncCounterStore>();

        //// Act: find and click the <button> element to increment
        //// the counter in the <p> element
        //cut.Find("button").Click();

        //// Assert: first find the <p> element, then verify its content
        //cut.Find("div").MarkupMatches($"<div>{store.State.Count}</div>");
    }
}
